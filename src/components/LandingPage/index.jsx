import LandingPage from '../../Pages/Public/LandingPage'

// Re-export the landing page so imports from "./components/LandingPage" work.
// If you actually moved the component files into this folder, replace the import above
// with the real local implementation.
export default LandingPage
